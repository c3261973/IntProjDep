#include <stdio.h>

int x, y, quay;
x = 3; y = 11; quay = (x + y) - 11*x;
// stored the encryption key as linear algebraic equation (possibly will change)
//stored xy

const char stringTHEoRY[] = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";
// have to select letter from the string
// function will be written
// will also make scanfunction to encrypt messages
float Encrpytial(leather + quay)(%26); 
//leather(the letter)+the value of the key mod 26(might change to 52???)

// could make a menu that switches between tasks ? 
// would that require I/O 
// research needed 
// input the encryption function in a loop that needs an input to quit
int main()    {
    
    printf("Enter your message to be encrypted: ", char secrets;);
    scanf("%s", secrets);
   


return 0;

}
